import DeveloperProfileView from './DeveloperProfileView.jsx';

/**
 * Full developer profile layout — hero, stats, skills, projects, and social links.
 */
export default function ProfileHeader(props) {
  return <DeveloperProfileView {...props} />;
}
