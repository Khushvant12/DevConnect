# Copy to .env for overrides, or rely on .env.development / .env.production
#
# Backend origin only — do NOT include /api (app adds it once).
# Local:
VITE_API_URL=http://localhost:5000
VITE_SOCKET_URL=http://localhost:5000
#
# Production (Vercel — override .env.production if needed):
# VITE_API_URL=https://devconnect-backend-opz0.onrender.com
# VITE_SOCKET_URL=https://devconnect-backend-opz0.onrender.com
